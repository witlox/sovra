version: 3

server: https://app.fossa.com

project:
  id: github.com/witlox/sovra
  name: sovra

analyze:
  modules:
    - path: .
      type: gomod

paths:
  exclude:
    - tests/
    - docs/
    - infrastructure/
    - vendor/
