apiVersion: v1
kind: Secret
metadata:
  name: sovra-secrets
  labels:
    app: sovra
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual secrets in production
  # Use kubectl create secret or external secrets management
  database.username: "sovra"
  database.password: "REPLACE_IN_PRODUCTION"
  vault.token: "REPLACE_IN_PRODUCTION"
